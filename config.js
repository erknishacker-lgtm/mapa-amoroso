/**
 * Preencha com suas chaves do Supabase (Project Settings → API).
 * A anon key é pública por design; a senha do admin fica no banco (schema.sql).
 *
 * Enquanto estiver vazio, o quiz funciona normalmente e o tracking só fica em silêncio.
 */
window.MAPA_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
  timezoneLabel: "America/Sao_Paulo",
};
